<%= content_for(:navigation) do %>

<% end %>

<% content_for(:header) do %>
  All Questions
<% end %>

<ul>
  <div id="question_list" >
    <%= render(@questions) %>
  </div>
</ul>


<% if user_signed_in? %>
  <p class="new_question"><%= link_to "New Question", new_question_path, id: "question_link", remote: true %></p>
<% else %>
  <p class="new_question"><%= link_to "New Question", new_question_path, id: "question_link" %></p>
<% end %>

